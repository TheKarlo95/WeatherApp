<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="BaseText">
        <item name="android:textColorHint">@color/hintText</item>
    </style>

    <style name="TextTitleLarge" parent="BaseText">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="TextTitle" parent="TextTitleLarge">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="TextTitleSmall" parent="TextTitleLarge">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TextNormal" parent="BaseText">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/secondaryText</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="TextBold" parent="TextNormal">
        <item name="android:fontFamily">sans-serif</item>
    </style>

</resources>
